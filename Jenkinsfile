pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello DevOps'
            }
        }

        stage('Checkout') {
            steps {
                echo 'Code downloaded from GitHub'
            }
        }
    }
}
